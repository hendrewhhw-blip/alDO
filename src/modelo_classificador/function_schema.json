{
  "name": "classificar_fala",
  "description": "Classifica se a fala é dirigida ao assistente (SIM) ou é fala do apresentador/ruído (NAO).",
  "parameters": {
    "type": "object",
    "properties": {
      "result": {
        "type": "string",
        "enum": [
          "SIM",
          "NAO"
        ],
        "description": "SIM se dirigida ao assistente, NAO caso contrário."
      }
    },
    "required": [
      "result"
    ]
  }
}